import { UserService } from '@modules/auth/services';
// import { MockUser, User } from '@testing/mocks';
import { Observable, of } from 'rxjs';


export class UserServiceStub  {
    // set user(user: User) {} s
    // get user$(): Observable<User> {
    //     return of(mockUser);
    // }
}
